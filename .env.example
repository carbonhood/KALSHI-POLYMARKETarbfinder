# Optional: IBKR Client Portal Gateway for live ForecastEx data
# IBKR_GATEWAY_URL=http://localhost:5000/v1/api

# Set FORECASTEX_USE_IBKR_GATEWAY=True in config.py when gateway is running

# Optional: OpenAI API key for on-demand LLM market extraction (scripts/enrich_market_cache.py)
# Scans never call the LLM — they only read llm_extraction_cache.sqlite when populated.
# OPENAI_API_KEY=sk-...
